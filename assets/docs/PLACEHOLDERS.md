# Asset Placeholders & Provenance

All diagrams in `assets/` are **repository-native SVGs** authored for CompliAGL.
They embed no third-party or copyrighted imagery and use only text and vector
primitives.

## Current assets

| Path | Purpose | Status |
|------|---------|--------|
| `assets/hero/hero-banner.svg` | README hero banner | Final (native SVG) |
| `assets/architecture/governance-boundary.svg` | Governance vs. execution boundary | Final (native SVG) |
| `assets/architecture/workflow.svg` | Execution governance workflow | Final (native SVG) |
| `assets/architecture/architecture.svg` | System architecture zones | Final (native SVG) |
| `assets/architecture/proof-lifecycle.svg` | AIProof lifecycle | Final (native SVG) |
| `assets/branding/favicon.svg` | Favicon / logo mark | Final (native SVG) |
| `assets/screenshots/` | Real product screenshots | **Pending real capture** — see that folder's README |

## Style tokens

- Background: `#0a0f24` → `#111a3a` (dark navy)
- Accent gradient: `#6366f1` (indigo) → `#22d3ee` (cyan)
- Text: `#f8fafc` / `#cbd5e1` / `#94a3b8`
- Typography: system sans-serif stack

Diagrams are intentionally simplified. The full canonical architecture lives in
the root `README.md` (Mermaid) and `docs/architecture.md`.
